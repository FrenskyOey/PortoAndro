package com.frensky.porto.values.color.semantic

import androidx.compose.ui.graphics.Color

class ColorBlue {
    // Used for information background color
    val blue_10 = Color(0xFFEAF0FE)

    // Border color for information elements and components (Alert)
    val blue_20 = Color(0xFF83A7FA)

    // Used when you want to indicate a information alerts
    val blue_30 = Color(0xFF306CF7)

    // Used only as hover color on informations color
    val blue_40 = Color(0xFF2453C2)

    // Used only as selected indication for a informations colors
    val blue_50 = Color(0xFF132B63)
}