package com.frensky.porto.values.color.semantic

import androidx.compose.ui.graphics.Color

class ColorRed {
    // Used for negative background color
    val red_10 = Color(0xFFF7D8D4)

    // Border color for negative elements and components (Alert)
    val red_20 = Color(0xFFE9948B)

    // Used when you want to indicate a negative action/state (delete, failed action, error)
    val red_30 = Color(0xFFDB4D3D)

    // Used only as hover color on negative color
    val red_40 = Color(0xFFAF3E31)

    // Used only as selected indication for a negative colors
    val red_50 = Color(0xFF832E25)
}
